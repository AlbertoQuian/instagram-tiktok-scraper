# ---------------------------------------------------------
# Instagram & TikTok Scraper – Python dependencies
# ---------------------------------------------------------
# Install:  pip install -r requirements.txt
# Then run: playwright install chromium
# ---------------------------------------------------------

# Scraping engines
yt-dlp>=2024.1.0                # TikTok video/metadata download
# curl_cffi 0.15 dropped the bundled curl-impersonate targets that yt-dlp uses;
# pin to <0.15 until upstream restores compatibility.
curl_cffi>=0.11,<0.15           # Browser-impersonation HTTP backend for yt-dlp
playwright>=1.40.0              # Headless Chromium (Instagram + screenshots)

# HTTP client
httpx>=0.24.0                   # Async-capable HTTP requests (Instagram API)

# Data processing
pandas>=2.0.0                   # CSV export and data manipulation
lingua-language-detector>=2.0.0 # Language detection (ISO 639-1)

# Local GUI
flask>=2.3.0                    # Browser-based user interface

# Testing
pytest>=7.0.0                   # Test framework

# Optional -----------------------------------------------
# TikTokApi>=6.3.0              # TikTok API fallback (requires ms_token)
